using UnityEngine;
using System.Collections;

public class MyPhysicsEngine : MonoBehaviour {

    public bool EnableFriction { get; set; } // enable static and dynamic friction for the system
	
	// Update is called once per frame
	void FixedUpdate () {
	
	}
}
